export type ExperienceType = {
  title: string;
  company_name: string;
  icon: string
  iconBg: string,
  date: string,
  points: string[]
}

export type SkillType = {
  imageUrl: string;
  name: string;
  type: string;
}